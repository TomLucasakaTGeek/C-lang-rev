#include <stdio.h>
void main()
{
  int n;
  printf("Enter a number");
  scanf("%d", &n);
  //for loops are generally used for known number of iterations
  for(int i=0;i<n;i++)   /* syntax of for 
    {                       (<1>initialization;<2>range;<4>increment)
    printf(i);                         <3>Procedure 
    }                     */
}
