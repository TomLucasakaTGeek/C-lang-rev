#include <stdio.h>
int main()
{
  int n;
  scanf("%d", &n);
//while loop is generally used for unknown number of operations
  while(n>0)     /* (range or condition)
 {                    procedure;
   n--;               incremment;
 }               */
return 0;
}
  
